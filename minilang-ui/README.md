# MiniLang UI

Interactive compiler inspector frontend for MiniLang IDE.

## Run

```bash
npm install
npm run dev
```

The interface includes the animated hero, editor, compiler tabs, command palette, compile/refactor/clear controls, AST interactions, and React Flow Automata/CFG graph.
