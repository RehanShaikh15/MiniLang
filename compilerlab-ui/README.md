# CompilerLab

Interactive compiler inspector frontend demo.

## Run

```bash
npm install
npm run dev
```

The pipeline and statistics strip have been removed from the UI. The remaining interface keeps the animated hero, editor, compiler tabs, command palette, compile/refactor/clear controls, AST interactions, and React Flow Automata/CFG graph.
